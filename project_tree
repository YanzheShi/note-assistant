note-assistant/
├── README.md                          # 项目说明（面试官看的）
├── pyproject.toml                     # 依赖管理（uv pip / pip install -e .）
├── .env.example                       # 环境变量模板
├── .env                               # 实际 Key（gitignore）
├── .gitignore
│
├── docker-compose.yml                 # Day 6 容器编排
├── Dockerfile.backend                 # FastAPI 容器
├── Dockerfile.frontend                # Streamlit 容器
│
├── data/
│   ├── vault/                         # Obsidian vault 的软链或复制（开发用）
│   ├── chroma/                        # ChromaDB 持久化目录
│   ├── sync.db                        # 增量索引状态
│   └── graph.gpickle                  # NetworkX 图缓存
│
├── models/
│   └── bge-reranker-v2-m3/            # 本地 reranker 模型 (~1.1GB)
│       ├── config.json
│       └── model.safetensors
│
├── src/
│   └── note_assistant/                # 主包（import note_assistant）
│       ├── __init__.py
│       │
│       ├── config.py                  # 全局配置（路径、模型名、参数）
│       │
│       ├── indexing/                  # 数据加载 + 切分 + 入库
│       │   ├── __init__.py
│       │   ├── vault_loader.py        # 读 .md → 提取 front_matter/wikilinks/标题
│       │   ├── preprocessor.py        # 富结构预处理（code fence 保护 + 抽 table/mermaid/image）
│       │   ├── splitter.py            # MarkdownHeaderTextSplitter + 递归后备
│       │   ├── embedder.py            # Ollama bge-m3 embedding 封装
│       │   ├── ingestor.py            # 入库 ChromaDB（两路 chunk）
│       │   └── sync.py                # sync.db 增量索引逻辑
│       │
│       ├── retrieval/                 # 检索 + 重排 + 扩展
│       │   ├── __init__.py
│       │   ├── dense_retriever.py     # ChromaDB 向量检索
│       │   ├── sparse_retriever.py    # BM25 封装
│       │   ├── hybrid.py              # 加权融合
│       │   ├── reranker.py            # bge-reranker-v2-m3 封装
│       │   ├── graph.py               # NetworkX 双链图构建 + 一跳扩展
│       │   └── query_rewrite.py       # LLM query 改写
│       │
│       ├── generation/                # LLM 生成
│       │   ├── __init__.py
│       │   ├── llm.py                 # DeepSeek / Qwen 切换封装
│       │   └── prompts.py             # Prompt 模板
│       │
│       ├── pipeline/                  # 端到端编排
│       │   ├── __init__.py
│       │   └── rag_chain.py           # 组装：改写→检索→rerank→扩展→生成→返回
│       │
│       ├── api/                       # FastAPI 后端
│       │   ├── __init__.py
│       │   ├── main.py                # /ask, /health, /reindex
│       │   └── schemas.py             # Pydantic 请求/响应模型
│       │
│       └── evaluation/                # 评测
│           ├── __init__.py
│           ├── qa_dataset.py          # 100 条 QA 加载/生成
│           └── ragas_eval.py          # Ragas 跑分
│
├── frontend/
│   ├── app.py                         # Streamlit 主入口
│   ├── components/
│   │   ├── chat.py                    # 聊天输入框 + 历史
│   │   ├── source_expander.py         # 来源折叠（按 type 区分渲染）
│   │   └── debug_panel.py             # 检索过程可视化（Day 5 晚）
│   └── utils.py                       # API 调用封装
│
├── scripts/
│   ├── run_index.py                   # 一键索引脚本
│   ├── run_eval.py                    # 一键评测脚本
│   └── run_dev.py                     # 本地开发启动（backend + frontend）
│
├── tests/                             # 单元测试（不要求全，关键路径覆盖）
│   ├── test_preprocessor.py
│   ├── test_splitter.py
│   └── test_pipeline.py
│
├── eval/
│   ├── qa.json                        # 100 条 QA 数据集
│   └── results/                       # Ragas 结果
│       └── run_2026-06-XX.csv
│
└── DEV_LOG.md                         # 开发日记